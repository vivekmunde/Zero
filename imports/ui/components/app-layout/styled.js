import styled from 'styled-components';

const StyledAppLayout = styled.div`
    padding-bottom: ${({ theme }) => theme.base.padding};
    box-sizing    : border-box;
`;

export default StyledAppLayout;